#!/usr/bin/env bash
<%= node['general']['template_header'] %>

f=$1
p=$2
wal_arch="<%= node['pg']['hadr']['wal_archive'] %>"
wal_tar="<%= node['pg']['root'] %>/active.tar/pg_wal.tar"

test ! -f $wal_arch/$f && cp $p $wal_arch/$f

test $wal_tar && tar -C $wal_arch -rf $wal_tar $f
